---
galaxy_info:
  description: apt keys role
  author: "arillso (@arillso)"
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - buster
        - bullseye
    - name: Ubuntu
      versions:
        - focal

  galaxy_tags:
    - key
    - apt

dependencies:
  - role: arillso.system.apt_cache
