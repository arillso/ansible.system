# File: roles/system_tuning/handlers/main.yml
---
- name: Reload kernel sysctl parameters from /etc/sysctl.d/
  ansible.builtin.command: sysctl --system
  register: _tuning_sysctl_reload_result
  changed_when: true
  failed_when: _tuning_sysctl_reload_result.rc != 0
  listen: "tuning: reload sysctl"

- name: Regenerate GRUB configuration after kernel command-line change
  ansible.builtin.command: "{{ _tuning_grub_update_commands[ansible_facts['os_family']] }}"
  register: _tuning_grub_update_result
  changed_when: true
  failed_when: _tuning_grub_update_result.rc != 0
  when: ansible_facts['os_family'] in _tuning_grub_update_commands
  listen: "tuning: update grub"

- name: Reload udev to apply new device rules
  ansible.builtin.systemd:
      name: systemd-udevd
      state: reloaded
  register: _tuning_udev_reload_result
  listen: "tuning: reload udev"

- name: Reload systemd manager configuration after tuning unit changes
  ansible.builtin.systemd:
      daemon_reload: true
  register: _tuning_systemd_reload_result
  failed_when: _tuning_systemd_reload_result.failed
  listen: "tuning: reload systemd"

# `tuning: apply` is a bundle event that triggers both the network-tuning
# and disable-thp service handlers. Individual events
# (`tuning: apply network`, `tuning: apply thp`) remain available for
# callers that only need one of them.

- name: Enable and start network-tuning systemd service
  ansible.builtin.systemd:
      name: network-tuning
      enabled: true
      state: started
      daemon_reload: true
  register: _tuning_network_service_result
  listen:
      - "tuning: apply"
      - "tuning: apply network"

- name: Enable and start disable-thp systemd service
  ansible.builtin.systemd:
      name: disable-thp
      enabled: true
      state: started
      daemon_reload: true
  register: _tuning_thp_service_result
  listen:
      - "tuning: apply"
      - "tuning: apply thp"

- name: Restart cpufrequtils to apply tuning-driven CPU governor changes (Debian)
  ansible.builtin.systemd:
      name: cpufrequtils
      state: restarted
  register: _tuning_cpufreq_restart_result
  when: ansible_facts['os_family'] == "Debian"
  listen: "tuning: restart cpufrequtils"

- name: Restart cpupower to apply CPU governor changes (RedHat/Suse)
  ansible.builtin.systemd:
      name: cpupower
      state: restarted
  register: _tuning_cpupower_restart_result
  when: ansible_facts['os_family'] in ["RedHat", "Suse"]
  listen: "tuning: restart cpupower"

- name: Validate grub update
  when: _tuning_grub_update_result is defined and _tuning_grub_update_result.rc == 0
  block:
      - name: Check if GRUB was updated successfully
        ansible.builtin.stat:
            path: "{{ '/boot/grub/grub.cfg' if ansible_facts['os_family'] == 'Debian' else '/boot/grub2/grub.cfg' }}"
        register: _tuning_grub_config_stat

      - name: Verify GRUB configuration timestamp
        ansible.builtin.debug:
            msg: "GRUB configuration updated at {{ _tuning_grub_config_stat.stat.mtime }}"
        when: _tuning_grub_config_stat.stat.exists

      - name: Warn about GRUB update failure
        ansible.builtin.debug:
            msg: "WARNING: GRUB configuration file not found. Manual verification required."
        when: not _tuning_grub_config_stat.stat.exists

- name: Validate service states
  block:
      # Read-only status probes — `failed_when: false` lets the report
      # task render gracefully when the unit is absent (e.g. the host
      # never had the corresponding write handler fire).
      - name: Check network tuning service status
        ansible.builtin.systemd:
            name: network-tuning
        register: _tuning_network_service_status
        failed_when: false

      - name: Check disable-thp service status
        ansible.builtin.systemd:
            name: disable-thp
        register: _tuning_thp_service_status
        failed_when: false

      - name: Report service status
        ansible.builtin.debug:
            msg:
                - "Service status summary:"
                - >-
                    - Network tuning: {{ 'Active'
                      if _tuning_network_service_status.status.ActiveState == 'active'
                      else 'Inactive' }}
                - >-
                    - Disable THP: {{ 'Active'
                      if _tuning_thp_service_status.status.ActiveState == 'active'
                      else 'Inactive' }}
        when:
            - _tuning_network_service_status is defined
            - _tuning_thp_service_status is defined
