---
# File: roles/system_tuning/tasks/swap.yml
# Robust swap file management with proper validation

- name: Validate swap configuration
  block:
      # Eine YAML-Liste unter `when:` wird AND-verknuepft — die drei
      # Bedingungen schliessen sich aber gegenseitig aus (undefined kann
      # nicht gleichzeitig `== ""` sein), sodass kein fail je feuern
      # wuerde. `assert` macht die OR-Semantik explizit und liest sich
      # gleichzeitig als Pre-Condition statt als if-then-fail.
      - name: Assert swap file path is valid
        ansible.builtin.assert:
            that:
                - tuning_swap_file_path is defined
                - tuning_swap_file_path | string | length > 0
                - tuning_swap_file_path is string and tuning_swap_file_path.startswith("/")
            fail_msg: "Invalid swap file path: {{ tuning_swap_file_path | default('<undefined>') }}"

      - name: Assert swap file size is valid
        ansible.builtin.assert:
            that:
                - tuning_swap_file_size_mb is defined
                - tuning_swap_file_size_mb | int >= 64
            fail_msg: "Invalid swap file size: {{ tuning_swap_file_size_mb | default('<undefined>') }}MB (minimum 64MB)"

- name: Check available disk space using facts
  block:
      - name: Get mount point for swap file directory
        ansible.builtin.set_fact:
            _tuning_swap_mount_point: >-
                {%- set swap_dir = tuning_swap_file_path | dirname -%}
                {%- for mount in ansible_mounts -%}
                {%- if swap_dir.startswith(mount.mount) -%}
                {{ mount.mount }}{%- endif -%}
                {%- endfor -%}

      - name: Get available space from mount facts
        ansible.builtin.set_fact:
            _tuning_available_space_mb: >-
                {%- for mount in ansible_mounts -%}
                {%- if mount.mount == _tuning_swap_mount_point -%}
                {{ (mount.size_available / 1024 / 1024) | int }}{%- endif -%}
                {%- endfor -%}

      - name: Ensure sufficient disk space
        ansible.builtin.fail:
            msg: "Insufficient disk space. Need {{ tuning_swap_file_size_mb }}MB, available {{ _tuning_available_space_mb }}MB"
        when: (_tuning_available_space_mb | int) < (tuning_swap_file_size_mb | int + 100)

- name: Check current swap status
  block:
      - name: Check if swap file exists
        ansible.builtin.stat:
            path: "{{ tuning_swap_file_path }}"
        register: _tuning_swap_file_stat

      - name: Get current swap information
        ansible.builtin.command: swapon --show=NAME,SIZE,PRIO --noheadings --bytes
        register: _tuning_active_swaps
        changed_when: false
        failed_when: false

      # Rundungs-Trick: swapon meldet die Swap-Datei-Grösse abzüglich des mkswap-Header-
      # Overheads (~4 KB). Eine reine Trunkierung würde z. B. 2048 MiB als 2047 MiB
      # zurückgeben und einen Recreate-Loop jeder Idempotenz verletzen.
      # Addieren von 524288 Bytes (= 0.5 MiB) vor der Division emuliert round() und
      # fängt den Header-Verlust ab.
      # _tuning_current_swap_priority bleibt "" wenn die Swap nicht aktiv ist; ein gesetzter
      # Wert erlaubt den Priority-Drift-Vergleich beim swapon weiter unten.
      - name: Parse active swap information
        ansible.builtin.set_fact:
            _tuning_swap_file_active: "{{ tuning_swap_file_path in _tuning_active_swaps.stdout }}"
            _tuning_current_swap_size: >-
                {%- for line in _tuning_active_swaps.stdout_lines -%}
                {%- if tuning_swap_file_path in line -%}
                {{ ((line.split()[1] | int + 524288) / 1024 / 1024) | int }}{%- endif -%}
                {%- endfor -%}
            _tuning_current_swap_priority: >-
                {%- for line in _tuning_active_swaps.stdout_lines -%}
                {%- if tuning_swap_file_path in line -%}
                {{ line.split()[2] }}{%- endif -%}
                {%- endfor -%}

      # Resize decision uses a ±1 MiB tolerance, not an exact match. swapon
      # reports the file size minus the mkswap header, and that overhead can
      # exceed the 0.5 MiB rounding correction above (it grows on small files
      # and varies by kernel), so an exact `current != target` comparison would
      # recreate a correctly-sized swap file on every run and break idempotency.
      - name: Determine whether the active swap file needs resizing
        ansible.builtin.set_fact:
            _tuning_swap_needs_resize: >-
                {{ _tuning_current_swap_size != ""
                   and ((_tuning_current_swap_size | int)
                        - (tuning_swap_file_size_mb | int)) | abs > 1 }}

- name: Create or resize swap file
  block:
      # Kein Backup der Swap-Datei: Swap enthält nur flüchtige Pages, eine Kopie
      # sichert nichts Verwertbares. Der frühere Backup-Task legte bei jedem
      # Resize eine 2-GB-Kopie an (.backup.<epoch>) — auf der kleinen code-01-
      # Disk füllte das die Partition, bis die "Ensure sufficient disk space"-
      # Assertion oben den ganzen Lauf failte. Die Resize-Sequenz unten
      # (swapoff → remove → fallocate) braucht kein Backup.
      - name: Refuse to shrink existing swap file
        ansible.builtin.fail:
            msg: >-
                Refusing to shrink swap file {{ tuning_swap_file_path }} from
                {{ _tuning_current_swap_size }}MB to {{ tuning_swap_file_size_mb }}MB.
                Disable explicitly via tuning_swap_file_enabled=false and rerun,
                then re-enable.
        when:
            - _tuning_swap_file_stat.stat.exists
            - _tuning_current_swap_size != ""
            - (_tuning_current_swap_size | int) > (tuning_swap_file_size_mb | int)

      # A resize is only warranted when the file is active at a known size that
      # differs from the target. An existing-but-inactive file (_tuning_current_swap_size
      # == "", e.g. after a reboot before fstab mounts it, or a manual swapoff)
      # must NOT be removed/recreated — that would break idempotency on every run.
      - name: Disable existing swap file if resizing
        ansible.builtin.command: swapoff {{ tuning_swap_file_path }}
        become: true
        changed_when: true
        when:
            - _tuning_swap_file_active
            - _tuning_swap_needs_resize | bool

      - name: Refresh _tuning_swap_file_active fact after swapoff
        ansible.builtin.set_fact:
            _tuning_swap_file_active: false
        when:
            - _tuning_swap_file_active
            - _tuning_swap_needs_resize | bool

      - name: Remove existing swap file if resizing
        ansible.builtin.file:
            path: "{{ tuning_swap_file_path }}"
            state: absent
        become: true
        when:
            - _tuning_swap_file_stat.stat.exists
            - _tuning_swap_needs_resize | bool

      - name: Create swap file using fallocate
        ansible.builtin.command: fallocate -l {{ tuning_swap_file_size_mb }}M {{ tuning_swap_file_path }}
        become: true
        register: _tuning_fallocate_result
        changed_when: _tuning_fallocate_result.rc == 0
        failed_when: false
        when: >-
            not _tuning_swap_file_stat.stat.exists or (_tuning_swap_needs_resize | bool)

      - name: Create swap file using dd (fallback)
        ansible.builtin.command: >-
            dd if=/dev/zero of={{ tuning_swap_file_path }}
            bs=1M count={{ tuning_swap_file_size_mb }}
        become: true
        register: _tuning_dd_result
        changed_when: _tuning_dd_result.rc == 0
        when:
            - _tuning_fallocate_result.rc is defined
            - _tuning_fallocate_result.rc != 0

      - name: Verify swap file was created
        ansible.builtin.stat:
            path: "{{ tuning_swap_file_path }}"
        register: _tuning_new_swap_file_stat

      - name: Fail if swap file creation failed
        ansible.builtin.fail:
            msg: "Failed to create swap file {{ tuning_swap_file_path }}"
        when: not _tuning_new_swap_file_stat.stat.exists

- name: Configure swap file
  block:
      - name: Set swap file permissions
        ansible.builtin.file:
            path: "{{ tuning_swap_file_path }}"
            mode: "0600"
            owner: root
            group: root
        become: true

      - name: Verify swap file size
        ansible.builtin.shell: |
            actual_size=$(stat -c%s {{ tuning_swap_file_path }})
            expected_size=$(({{ tuning_swap_file_size_mb }} * 1024 * 1024))
            if [ $actual_size -ne $expected_size ]; then
                echo "Size mismatch: expected $expected_size, got $actual_size"
                exit 1
            fi
        register: _tuning_size_check
        changed_when: false

      # Format only a freshly created file: when it did not exist, or when it was
      # removed for a resize. Re-running mkswap on an existing, correctly-sized
      # file (incl. the inactive case) would report changed on every run.
      - name: Format swap file
        ansible.builtin.command: mkswap {{ tuning_swap_file_path }}
        become: true
        register: _tuning_mkswap_result
        changed_when: _tuning_mkswap_result.rc == 0
        when: >-
            not _tuning_swap_file_stat.stat.exists or (_tuning_swap_needs_resize | bool)

      - name: Enable swap file
        ansible.builtin.command: swapon -p {{ tuning_swap_priority }} {{ tuning_swap_file_path }}
        become: true
        register: _tuning_swapon_result
        changed_when: _tuning_swapon_result.rc == 0
        when: not _tuning_swap_file_active

      # A priority-only change on an already-active swap is not applied by a bare
      # `swapon -p` (the kernel rejects re-adding an active device), so reconcile
      # it explicitly: swapoff then swapon with the desired priority. Gated on a
      # real drift so correctly-prioritised hosts stay unchanged.
      - name: Reapply swap priority when it drifts on an active swap
        ansible.builtin.shell: >-
            swapoff {{ tuning_swap_file_path }}
            && swapon -p {{ tuning_swap_priority }} {{ tuning_swap_file_path }}
        become: true
        register: _tuning_swap_reprio_result
        changed_when: _tuning_swap_reprio_result.rc == 0
        when:
            - _tuning_swap_file_active
            - _tuning_current_swap_priority != ""
            - (_tuning_current_swap_priority | int) != (tuning_swap_priority | int)

      - name: Verify swap is active
        ansible.builtin.command: swapon --show=NAME --noheadings
        register: _tuning_verify_swap
        changed_when: false

      - name: Fail if swap activation failed
        ansible.builtin.fail:
            msg: "Swap file {{ tuning_swap_file_path }} is not active after swapon"
        when: tuning_swap_file_path not in _tuning_verify_swap.stdout

- name: Configure swap persistence
  block:
      # Run unconditionally: ansible.posix.mount is idempotent and reconciles the
      # opts. A previous "skip when the entry exists" gate meant a host with an
      # older `... sw 0 0` line never had pri={{ tuning_swap_priority }} persisted,
      # so the zram-first fallback layout would not survive a reboot on upgrade.
      - name: Ensure swap file is in fstab with the desired priority
        ansible.posix.mount:
            path: none
            src: "{{ tuning_swap_file_path }}"
            fstype: swap
            opts: "sw,pri={{ tuning_swap_priority }}"
            passno: 0
            dump: 0
            state: present
        become: true

- name: Report swap configuration status
  ansible.builtin.debug:
      msg:
          - "Swap file configuration completed:"
          - "  - File: {{ tuning_swap_file_path }}"
          - "  - Size: {{ tuning_swap_file_size_mb }}MB"
          - "  - Status: {{ 'Active' if tuning_swap_file_path in _tuning_verify_swap.stdout else 'Inactive' }}"
          - "  - Persistent: Added to /etc/fstab"
