# Configuration for cpufrequtils
# Generated by Ansible system tuning role
# CPU: {{ _tuning_detected_cpu_info.model | default('unknown') }}
# Vendor: {{ _tuning_detected_cpu_info.vendor | default('unknown') }}

# CPU frequency governor ({{ _tuning_final_cpu_governor }} selected for {{ _tuning_detected_cpu_info.vendor | default('unknown') }} CPU)
GOVERNOR="{{ _tuning_final_cpu_governor | default(tuning_cpu_governor) }}"

# Maximum CPU frequency (0 = auto-detect)
MAX_SPEED="0"

# Minimum CPU frequency (0 = auto-detect)
MIN_SPEED="0"

# CPU cores to manage (empty = all cores)
CPUS=""

# Additional options for cpufreq-set
OPTIONS=""
